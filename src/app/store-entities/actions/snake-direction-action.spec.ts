import { SnakeDirectionAction } from './snake-direction-action';

describe('SnakeDirectionAction', () => {
  it('should create an instance', () => {
    expect(new SnakeDirectionAction()).toBeTruthy();
  });
});
